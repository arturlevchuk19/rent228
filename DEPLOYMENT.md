# RentMaster2 - Руководство по развертыванию

## 📋 Содержание

1. [Требования](#требования)
2. [Быстрый старт](#быстрый-старт)
3. [Настройка Supabase](#настройка-supabase)
4. [Выполнение миграций](#выполнение-миграций)
5. [Настройка проекта](#настройка-проекта)
6. [Запуск приложения](#запуск-приложения)
7. [Создание первого пользователя](#создание-первого-пользователя)
8. [Развертывание в production](#развертывание-в-production)
9. [Решение проблем](#решение-проблем)

---

## 🎯 Требования

### Обязательные

- **Node.js** >= 18.0.0
- **npm** >= 9.0.0 или **yarn** >= 1.22.0
- **Аккаунт Supabase** (бесплатный: https://supabase.com)

### Рекомендуемые

- **Supabase CLI** - для управления миграциями
- **Git** - для контроля версий
- **VS Code** - с расширениями ESLint и Prettier

---

## 🚀 Быстрый старт

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd RentMaster2
```

### 2. Установка зависимостей

```bash
npm install
```

### 3. Настройка переменных окружения

```bash
cp .env.example .env
```

Отредактируйте `.env` и укажите ваши Supabase credentials:

```env
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
```

### 4. Выполнение миграций базы данных

```bash
cd supabase
./run-migrations.sh
```

### 5. Запуск приложения

```bash
npm run dev
```

Приложение будет доступно по адресу: http://localhost:5173

---

## ⚙️ Настройка Supabase

### Шаг 1: Создание проекта

1. Перейдите на https://supabase.com
2. Войдите или создайте аккаунт
3. Нажмите "New Project"
4. Заполните данные:
   - **Name**: RentMaster2
   - **Database Password**: (сохраните пароль)
   - **Region**: выберите ближайший регион
   - **Pricing Plan**: Free tier (для начала)

### Шаг 2: Получение API ключей

1. В Dashboard перейдите в **Settings** → **API**
2. Скопируйте:
   - **Project URL** → `VITE_SUPABASE_URL`
   - **anon/public key** → `VITE_SUPABASE_ANON_KEY`

### Шаг 3: Настройка Authentication

1. Перейдите в **Authentication** → **Settings**
2. Настройте:
   - **Site URL**: `http://localhost:5173` (для разработки)
   - **Redirect URLs**: добавьте `http://localhost:5173`
3. В разделе **Email Auth**:
   - Включите **Enable Email Confirmations** (опционально)
   - Настройте SMTP (опционально, для production)

---

## 🗄️ Выполнение миграций

### Метод 1: Supabase CLI (Рекомендуется)

#### Установка Supabase CLI

```bash
npm install -g supabase
```

#### Авторизация

```bash
supabase login
```

#### Связывание с проектом

```bash
supabase link --project-ref your-project-ref
```

Project ref можно найти в URL вашего проекта: `https://YOUR-PROJECT-REF.supabase.co`

#### Выполнение миграций

```bash
cd supabase
supabase db push
```

### Метод 2: Bash скрипт

```bash
cd supabase
chmod +x run-migrations.sh
./run-migrations.sh
```

### Метод 3: SQL Editor в Supabase Dashboard

1. Откройте **SQL Editor** в Supabase Dashboard
2. Для каждого файла в `supabase/migrations/` (в алфавитном порядке):
   - Откройте файл
   - Скопируйте содержимое
   - Вставьте в SQL Editor
   - Нажмите **Run**

### Проверка миграций

```bash
supabase db diff --schema public
```

Или проверьте в Dashboard → **Database** → **Tables**

---

## 🔧 Настройка проекта

### Структура проекта

```
RentMaster2/
├── src/                    # Исходный код приложения
│   ├── components/        # React компоненты
│   ├── contexts/          # React contexts (Auth)
│   ├── lib/              # Библиотеки и API клиенты
│   ├── pages/            # Страницы приложения
│   ├── App.tsx           # Главный компонент
│   └── main.tsx          # Точка входа
├── supabase/              # Конфигурация Supabase
│   ├── migrations/       # SQL миграции
│   ├── config.toml       # Конфигурация
│   ├── run-migrations.sh # Bash скрипт миграций
│   └── run-migrations.js # Node.js скрипт миграций
├── .env                   # Переменные окружения (НЕ коммитить!)
├── .env.example          # Шаблон для .env
├── package.json          # Зависимости проекта
└── vite.config.ts        # Конфигурация Vite
```

### Настройка .gitignore

Убедитесь, что `.env` добавлен в `.gitignore`:

```gitignore
# Environment variables
.env
.env.local
.env.production

# Dependencies
node_modules/

# Build
dist/
```

---

## ▶️ Запуск приложения

### Режим разработки

```bash
npm run dev
```

Сервер запустится на http://localhost:5173 с hot-reload

### Сборка для production

```bash
npm run build
```

Результат будет в папке `dist/`

### Предварительный просмотр production сборки

```bash
npm run preview
```

### Проверка типов

```bash
npm run type-check
```

### Линтинг

```bash
npm run lint
```

---

## 👤 Создание первого пользователя

### Через приложение

1. Запустите приложение: `npm run dev`
2. Откройте http://localhost:5173
3. Нажмите "Регистрация"
4. Заполните форму:
   - Email
   - Пароль
   - Полное имя
5. Подтвердите email (если включены email confirmations)

### Назначение роли superuser

После регистрации необходимо вручную назначить роль через SQL Editor:

```sql
-- Найдите ID нового пользователя
SELECT id, email FROM auth.users ORDER BY created_at DESC LIMIT 5;

-- Создайте запись в таблице users
INSERT INTO public.users (id, email, full_name, role, status)
VALUES (
  'user-id-from-above',
  'user@example.com',
  'Admin User',
  'superuser',
  'active'
);
```

### Через Supabase Dashboard

1. **Authentication** → **Users** → Найдите пользователя
2. **Database** → **Table Editor** → **users**
3. Добавьте запись с соответствующим `id` из `auth.users`
4. Установите:
   - `role`: `superuser`
   - `status`: `active`

---

## 🌐 Развертывание в production

### Подготовка

1. **Обновите переменные окружения** для production:

```env
VITE_SUPABASE_URL=https://your-production-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-production-anon-key
```

2. **Настройте Supabase для production**:
   - Authentication → Settings → Site URL: `https://yourdomain.com`
   - Authentication → Settings → Redirect URLs: добавьте production URL

3. **Выполните миграции на production базе**:

```bash
supabase link --project-ref production-project-ref
supabase db push
```

### Варианты хостинга

#### 1. Vercel (Рекомендуется)

```bash
npm install -g vercel
vercel login
vercel
```

Добавьте environment variables в Vercel Dashboard.

#### 2. Netlify

```bash
npm install -g netlify-cli
netlify login
netlify deploy --prod
```

Или подключите через Git integration.

#### 3. GitHub Pages (только для статических сайтов)

```bash
npm run build
# Разместите содержимое dist/ на GitHub Pages
```

#### 4. Собственный VPS

```bash
# Установите Node.js и Nginx
npm run build
# Настройте Nginx для обслуживания dist/
# Настройте SSL с помощью Let's Encrypt
```

---

## 🔧 Решение проблем

### Ошибки подключения к Supabase

**Проблема**: `Missing Supabase environment variables`

**Решение**:
1. Проверьте, что `.env` файл существует
2. Убедитесь, что переменные начинаются с `VITE_`
3. Перезапустите dev сервер после изменения `.env`

### Ошибки аутентификации

**Проблема**: Не удается войти после регистрации

**Решение**:
1. Проверьте, что запись создана в таблице `users`
2. Убедитесь, что `role` и `status` установлены корректно
3. Проверьте RLS policies в Supabase

### Ошибки миграций

**Проблема**: Миграция не выполняется

**Решение**:
1. Проверьте порядок миграций (они должны выполняться в алфавитном порядке)
2. Убедитесь, что нет конфликтов с существующими таблицами
3. Проверьте логи в Supabase Dashboard → Database → Logs

### Проблемы с CORS

**Проблема**: CORS ошибки при запросах к Supabase

**Решение**:
1. Проверьте, что используете правильный `anon key`
2. Убедитесь, что домен добавлен в Supabase → Authentication → Settings → Redirect URLs

### Проблемы с RLS (Row Level Security)

**Проблема**: Не удается получить доступ к данным

**Решение**:
1. Проверьте RLS policies в Supabase Dashboard
2. Убедитесь, что пользователь имеет правильную роль
3. Временно отключите RLS для отладки (не рекомендуется для production)

---

## 📚 Дополнительные ресурсы

- **Документация Supabase**: https://supabase.com/docs
- **Документация Vite**: https://vitejs.dev
- **Документация React**: https://react.dev
- **Документация TypeScript**: https://www.typescriptlang.org/docs

---

## 🆘 Поддержка

Если у вас возникли проблемы, которые не описаны в этом руководстве:

1. Проверьте логи в консоли браузера (F12)
2. Проверьте логи в Supabase Dashboard
3. Создайте issue в репозитории проекта

---

## 📝 Чек-лист развертывания

- [ ] Node.js и npm установлены
- [ ] Создан проект в Supabase
- [ ] Получены API ключи
- [ ] Создан `.env` файл
- [ ] Установлены зависимости (`npm install`)
- [ ] Выполнены миграции базы данных
- [ ] Создан первый пользователь с ролью superuser
- [ ] Приложение запускается локально
- [ ] Проверена работа аутентификации
- [ ] Проверена работа основных функций
- [ ] (Production) Настроены production переменные окружения
- [ ] (Production) Выполнены миграции на production базе
- [ ] (Production) Настроен хостинг
- [ ] (Production) Настроен SSL сертификат

---

**Версия документа**: 1.0  
**Дата обновления**: 04.02.2026  
**Автор**: RentMaster2 Team

# RentMaster2 - Быстрый старт 🚀

Краткое руководство для быстрого развертывания проекта.

## 📦 Шаг 1: Установка зависимостей

```bash
npm install
```

## ⚙️ Шаг 2: Настройка Supabase

### 2.1 Создание проекта

1. Перейдите на https://supabase.com и войдите/зарегистрируйтесь
2. Нажмите "New Project"
3. Заполните:
   - **Name**: RentMaster2
   - **Database Password**: (создайте и сохраните надежный пароль)
   - **Region**: выберите ближайший к вам регион
4. Нажмите "Create new project" и дождитесь завершения

### 2.2 Получение API ключей

1. В Dashboard вашего проекта перейдите в **Settings** → **API**
2. Скопируйте следующие значения:
   - **Project URL**: `https://xxxxx.supabase.co`
   - **anon public**: начинается с `eyJhbG...`

## 🔑 Шаг 3: Настройка переменных окружения

```bash
# Создать .env из шаблона
cp .env.example .env
```

Откройте `.env` и замените значения на свои:

```env
VITE_SUPABASE_URL=https://ваш-проект.supabase.co
VITE_SUPABASE_ANON_KEY=ваш-anon-key
```

## 🗄️ Шаг 4: Выполнение миграций базы данных

### Вариант A: Через Supabase CLI (рекомендуется)

```bash
# Установка CLI (если еще не установлен)
npm install -g supabase

# Авторизация
supabase login

# Связывание с проектом
supabase link --project-ref ваш-project-ref

# Выполнение миграций
supabase db push
```

**Project ref** можно найти в URL проекта: `https://ВАШИ-СИМВОЛЫ.supabase.co`

### Вариант B: Через bash скрипт

```bash
cd supabase
chmod +x run-migrations.sh
./run-migrations.sh
cd ..
```

### Вариант C: Через Supabase Dashboard (ручной метод)

1. Откройте **SQL Editor** в Supabase Dashboard
2. Для каждого файла в `supabase/migrations/` (в алфавитном порядке):
   - Откройте файл в текстовом редакторе
   - Скопируйте всё содержимое
   - Вставьте в SQL Editor
   - Нажмите **Run**
   - Дождитесь выполнения перед следующим файлом

## ✅ Шаг 5: Проверка окружения

```bash
npm run check
```

Эта команда проверит:
- ✓ Версию Node.js
- ✓ Установленные зависимости
- ✓ Переменные окружения
- ✓ Файлы Supabase

## 🚀 Шаг 6: Запуск приложения

```bash
npm run dev
```

Приложение запустится на http://localhost:5173

## 👤 Шаг 7: Создание первого пользователя

### 7.1 Регистрация

1. Откройте http://localhost:5173
2. Нажмите "Регистрация"
3. Заполните форму:
   - Email
   - Пароль (минимум 6 символов)
   - Полное имя
4. Нажмите "Зарегистрироваться"

### 7.2 Назначение роли Superuser

После регистрации необходимо вручную назначить роль через SQL Editor:

1. Откройте Supabase Dashboard → **SQL Editor**
2. Выполните:

```sql
-- Найдите ID вашего пользователя
SELECT id, email FROM auth.users 
ORDER BY created_at DESC 
LIMIT 1;

-- Создайте запись в public.users (замените значения)
INSERT INTO public.users (id, email, full_name, role, status)
VALUES (
  'your-user-id-from-above',
  'your@email.com',
  'Admin User',
  'superuser',
  'active'
);
```

### 7.3 Вход в систему

1. Вернитесь в приложение http://localhost:5173
2. Если уже авторизованы - обновите страницу
3. Если нет - войдите с вашими credentials
4. Теперь у вас полный доступ ко всем функциям!

## 🎉 Готово!

Теперь вы можете:
- ✅ Добавлять оборудование
- ✅ Создавать клиентов и площадки
- ✅ Планировать мероприятия
- ✅ Создавать сметы
- ✅ Управлять персоналом
- ✅ Генерировать отчеты

## 📚 Что дальше?

- 📖 Прочитайте [DEPLOYMENT.md](./DEPLOYMENT.md) для подробной информации
- 📊 Изучите [DATABASE_SCHEMA.md](./supabase/DATABASE_SCHEMA.md) для понимания структуры БД
- 🔧 Настройте систему под свои нужды

## 🆘 Возникли проблемы?

### Не удается подключиться к Supabase

```bash
# Проверьте .env файл
cat .env

# Убедитесь, что переменные начинаются с VITE_
# Перезапустите dev сервер
```

### Ошибки при миграциях

```bash
# Проверьте логи в Supabase Dashboard
# Database → Logs

# Убедитесь, что выполняете миграции в правильном порядке
ls -la supabase/migrations/
```

### Не удается войти после регистрации

```sql
-- Проверьте, что запись создана в public.users
SELECT * FROM public.users WHERE email = 'your@email.com';

-- Если нет, создайте вручную (см. Шаг 7.2)
```

### Другие проблемы

- Проверьте консоль браузера (F12)
- Проверьте логи в терминале где запущен `npm run dev`
- Прочитайте раздел "Решение проблем" в [DEPLOYMENT.md](./DEPLOYMENT.md)

## 🔗 Полезные команды

```bash
# Проверка окружения
npm run check

# Запуск dev сервера
npm run dev

# Сборка для production
npm run build

# Предварительный просмотр production
npm run preview

# Проверка типов TypeScript
npm run typecheck

# Линтинг кода
npm run lint

# Статус миграций (требует Supabase CLI)
npm run db:status
```

## 📞 Поддержка

Если что-то не работает:
1. Перечитайте этот гайд внимательно
2. Проверьте [DEPLOYMENT.md](./DEPLOYMENT.md)
3. Посмотрите логи в консоли
4. Создайте issue в репозитории

---

**Приятной работы с RentMaster2! 🎭**
